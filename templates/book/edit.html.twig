{% extends 'base.html.twig' %}

{% block title %}Edit Book{% endblock %}

{% block body %}
<div class="bookPageEdit">
    {% include 'header/index.html.twig' %}
    <h1>Edit Book {{book.title|raw}}</h1>
    {{ form(form) }}
    

    <a href="{{ path('app_book') }}">Back to list</a>
</div>
{% endblock %}
